<%= turbo_stream.replace "category-#{@category.id}" do %>
  <%= render "trips/category_card", category: @category, trip_items: @category_items %>
<% end %>

<%= turbo_stream.replace "trip-progress" do %>
  <%= render "trips/progress", trip: @trip %>
<% end %>
